services:
  db:
    image: pgvector/pgvector:pg17
    container_name: info-agent-db
    environment:
      POSTGRES_DB: info_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
